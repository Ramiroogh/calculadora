{
  "scripts": {
    "build:tailwind": "npx tailwindcss -i ./src/styles.css -o ./dist/output.css",
    "predeploy": "npm run build:tailwind",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "tailwindcss": "^3.3.2"
  }
}
